extends layout

block content

  #divEvaluacionesGeneral.col-sm-12
      .my-content
        .container
          .row
            .col-lg-12.col-lg-offset-3.myform-cont
              .myform-top
                .myform-top-left
                  h3 Listado de Evaluaciones
              .myform-bottom
                #divEvaluaciones.container
                  table#tablaEvaluaciones.table.table-hover
                    tbody
                      each val in evaluaciones
                        tr
                          td #{val.evaluacion.nombre}
                          td #{val.evaluacion.descripcion}
                          if val.evaluacion_completa
                            td Nota: #{val.nota}
                            td Observacion: #{val.observaciones}

                          else
                            td No Evaluado
                            td
                              button.btnEvaluaciones.btn.btn-primary.mr-2(id=val._id, type="submit") Evaluar

block scripts-extra
  script(src='/javascripts/evaluacionesEvaluador.js')
  script(src='/javascripts/tableResize.js')
